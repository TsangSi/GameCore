import { js } from "cc";
import { ItemViewBase } from "./ItemViewBase";

export class ItemViewEquip extends ItemViewBase {
    clear() {
        // this.Items = js.createMap(true);
    }

    protected addItem(index: number, id: string) {

    }
}
