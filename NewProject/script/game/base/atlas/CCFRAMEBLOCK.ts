const { ccclass, property, menu } = cc._decorator;

@ccclass
export default class CCFRAMEBLOCK extends cc.Component {
    // @property(cc.String)
    // private key: string = '';
    // 有一些特殊的无需加入的 就不要用这个渲染
}
